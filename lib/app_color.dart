import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(211, 187, 255, 1);

class AppColor {
  static const Color primary = primaryColor;
  static const Color appBar = primary;
  static const Color separator = Color.fromRGBO(245, 245, 245, 1);
}
